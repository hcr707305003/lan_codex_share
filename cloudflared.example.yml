# Local-managed Tunnel example. Replace placeholders before starting.
# Never commit the real credentials JSON.
tunnel: replace-with-your-tunnel-id
credentials-file: ./tunnel-credentials.json
ingress:
  - hostname: codex.example.com
    service: http://localhost:9000
  - service: http_status:404
