# 桌面控制台

从 v0.2.0 起，版本包包含桌面控制台。界面包含服务总览、配置管理、组件管理和运行日志。打开窗口不会自动连接任何隧道，三个服务分别手动启停；已从脚本启动的 Share 可被识别并单独确认后强制关闭，匹配当前配置的单个外部 FRP 普通进程也可确认关闭。Cloudflare 和 Windows 隧道服务只读检测，不自动接管。

## 从源码启动

使用独立 **CPython 3.11+** 创建环境。部分 Anaconda 环境会有 Qt DLL 冲突；不要替换系统 DLL，请使用独立 CPython 或打包后的版本。

Windows PowerShell：

```powershell
python -m venv .venv-desktop
.\.venv-desktop\Scripts\python.exe -m pip install -r requirements-desktop.txt
.\start_lan_codex_desktop.cmd
.\start_lan_codex_desktop.cmd --config=./team/lan_config.toml
```

macOS / Linux：

```sh
python3 -m venv .venv-desktop
.venv-desktop/bin/python -m pip install -r requirements-desktop.txt
bash start_lan_codex_desktop.sh --config=./lan_config.toml
```

原有 CLI/Share 不要求 Qt 依赖。上游 Codex CLI 仍需在本机安装并登录。

## 配置文件

| 文件 | 用途 |
| --- | --- |
| `lan_config.toml` | Share 端口、Session、权限、项目密码及两个公网入口 |
| `desktop_config.toml` | 程序路径、其他配置路径、Cloudflare 启动模式 |
| `frpc.toml` | frps 地址、`auth.token`、TLS 和 TCP 代理 |
| `cloudflared.yml` | 本地管理 Tunnel 的 YAML 与凭证文件位置 |
| 用户选择的 token 文本文件 | 远程管理 Tunnel 的 token，仅通过 `--token-file` 传递 |

默认 LAN 配置在程序发行目录，macOS 在 `.app` 外侧；显式相对 `--config` 按终端当前目录解析。桌面设置跟随所选 LAN 配置目录，其中的相对路径按设置文件目录解析。

Share 支持基础表单和原始 TOML，frpc 支持选择 TCP 代理后修改表单；Cloudflare YAML 支持常用字段表单与高级编辑。`session_ids` 缺省、空数组和指定数组保留原有不同含义。

三组表单的字段下方均显示配置键名和用途说明，说明会自动换行、跟随主题，并提供给辅助阅读工具。FRP 的控制/本地/公网端口、认证 Token 与项目密码、TLS 的加密范围分别说明；说明不包含实际密码或凭证内容，也不会写入配置文件。

点击“高级文件编辑”可直接修改完整 TOML/YAML 文本，包含表单未列出的配置项和注释；点击“返回表单编辑”切回。切换模式不写文件，仍需“校验并保存”。语法错误会保留输入并阻止保存；高级编辑会显示明文密码和 Token，请勿直接分享截图。

### Cloudflare 表单

选择“配置管理 → Cloudflare YAML”，常规配置默认显示 Tunnel ID/名称、凭证 JSON 文件、公网域名和本地回源地址。凭证可手填路径或点击“选择文件…”；文件选择器写入绝对路径，避免受进程工作目录影响，不读取或展示 JSON 内容。域名只填 `codex.example.com`，不带协议和路径。

“使用 Share 地址”从已保存的 Share 配置读取端口，将 `http://localhost:端口` 填入当前回源草稿；创建 YAML 示例也使用该端口。打开已有配置不会覆盖原回源。多条已有 HTTP(S) 域名入口可通过“入口规则”选择，切换前保留上一条草稿；其他协议、catch-all、路径匹配和 `originRequest` 等规则不被重建。仅修改编辑过的标量，保留注释及未知字段。

表单校验失败会保留输入并定位错误字段；复杂 YAML（例如锚点、别名或没有可编辑 HTTP(S) 域名入口）保留高级编辑并提示原因。Token 模式仍可编辑本地 YAML，但不会修改远程 Tunnel 的回源或切换启动模式。点击“校验并保存”后才写文件，仍沿用公网入口同步和外部修改冲突检查；保存不会重启服务。

“工作目录”通过“选择文件夹…”选择现有目录，左侧路径栏只读、可复制。对话框优先打开当前工作目录，无效时从配置文件目录打开；取消或重复选择同一目录不改动配置。新路径优先相对 `lan_config.toml` 所在目录保存，跨盘保存绝对路径，原有值未操作时保持原样。点击“校验并保存”才写入配置，不会自动重启 Share；需要手动填写时可切到原始 TOML 编辑。

Share 表单的“任务结束提示”开关对应 `notify_on_task_complete`，默认关闭。开启并保存、手动重启 Share 后刷新网页，当前 Session 任务结束会显示需点击 × 关闭的常驻提示；不同浏览器独立关闭，初次历史不补弹。

“预览目录”显示为目录列表，可通过“添加目录…”逐次选取、选中后移除，自动去重。新增路径以 `lan_config.toml` 所在目录为基准优先保存相对路径，跨磁盘保存绝对路径；旧路径保持原样。添加/移除只修改未保存表单，不删除磁盘目录，也不自动保存。

“Session 共享范围”支持三种模式：

- **指定会话**：每行一个 ID，可添加、复制选中 ID、移除选中项。添加支持单个 ID、换行/逗号分隔或 JSON 字符串数组，去除首尾空白、去重并保留顺序；无效批次不会部分加入。
- **共享全部**：保存 `session_ids = []`，按项目发现所有可共享会话，请确认访问范围。
- **自动单会话**：不配置 `session_ids` 和 `session_id`，使用原有的单会话复用/创建行为。

移除只改变共享配置，不删除真实 Session 或聊天历史。指定会话删除至空时不能保存，也不会自动切成共享全部。输入框中尚未添加的 ID 会计入未保存修改；请先添加或清空后再保存、切换模式或进入原始编辑。旧 `session_id` 在仅修改其他配置时保持原样，明确修改 Session 设置后才转为新配置格式。点击“校验并保存”才写入文件，运行中的 Share 需手动重启生效。

保存保留 TOML 注释与未知字段，检测外部文件修改。保存不会自动重启服务。原始编辑可能显示密钥，分享截图前请检查。私有配置、程序、日志和 token 文件不要提交仓库。

FRP 表单的“代理本地端口”旁提供“应用 Share 端口”：读取已保存 Share 配置的当前端口，只更新所选 TCP 代理的 `localPort` 草稿，不改变本地地址、公网端口、控制端口或其他代理。仍可手动输入；源配置不可用时保留原值并提示。点击“校验并保存”才写入，运行中的 frpc 需手动重启生效。

默认私有文件名已加入 `.gitignore`；使用自定义配置或 token 文件名时，请自行补充忽略规则。“另存副本”不会切换当前服务使用的配置。

### 修改 Share 端口时同步隧道回源

在控制台修改并保存已有 Share 配置的 Web 端口时，会同时尝试更新当前选定的 `frpc.toml` 和 `cloudflared.yml`；表单和高级文件编辑模式均支持：

- FRP：仅修改明确指向原 Share 端口的 TCP 本地回环代理 `localPort`，不改代理名称、`serverPort`、`remotePort` 或认证信息，也不处理 plugin 代理。
- Cloudflare 本地 YAML：仅修改指向原 Share 端口的 `http://localhost`、`http://127.0.0.1` 或 `http://[::1]` 回源 `service` 端口，保留域名、路径、其他规则及注释。带认证、查询、片段的回源不自动修改；包含 YAML 锚点/别名时提示手动检查。
- Cloudflare Token 模式：回源由远程管理，需在 Cloudflare 控制台修改，本地不会写 YAML。

两份隧道配置独立处理：缺失、格式错误、没有匹配规则、不可写或保存期间被外部修改时，提示对应的“未同步”原因，不覆盖其他服务配置，也不撤销已保存的 Share。失败后请按提示手动校正隧道端口；再次保存未改变端口的 Share 不会猜测原回源。新建 Share 配置、端口未改变或“另存副本”不触发联动。

同步只修改配置文件。公网入口不变，运行中的 Share 和受影响的隧道需要手动重启后生效；不会自动启动、停止或重启服务。

### 保存隧道配置时同步公网入口

控制台新建 FRP 示例时，代理名称自动生成为 `codex-share-<随机 UUID>`，降低多台客户端使用同一默认名称的冲突概率。已有配置不会自动改名；FRP 表单中可复制名称，或点击“重新生成”修改当前 TCP 代理的名称草稿，点击保存后才写入。生成器排除当前配置内已有名称，但不会连接 frps 验证全局唯一。不同代理仍须使用不同公网端口；名称变更在手动重启 frpc 后生效。原始 TOML 编辑仍可手动命名。

在控制台选中 `frpc.toml` 或 `Cloudflare YAML`，点击“校验并保存”后，会尝试同步当前 `lan_config.toml`：

- FRP：仅识别回源本机回环地址、`localPort` 等于 Share Web 端口的普通 TCP 映射，使用 `serverAddr` 的 IPv4 地址与 `remotePort` 生成 `frp_origin`；不会使用控制端口 `serverPort` 或其他服务的映射。现有 HTTP 公网入口只支持 IPv4，域名和公网 IPv6 不自动推导。
- Cloudflare 本地 YAML：仅识别回源 `http://本机回环地址:Share端口`、没有路径限制且使用明确域名的 ingress 规则，生成 `cloudflare_origin`。其他回源、通配域名及 catch-all 不参与同步。
- Token 模式无法仅凭 token 确定域名，请在 Share 表单中手动填写 Cloudflare 入口。程序不读取 token 内容来推导域名，也不请求远程配置。

只有一个候选且原入口为空时自动同步；相同入口不重复写入；多个候选或已有不同入口时弹出选择框，默认取消同步。旧 `public_origin` 会按原兼容规则迁移到对应字段并保留另一入口，不直接丢弃旧值。取消同步不撤销已保存的隧道配置。

HTTP 入口仍要求项目密码。LAN 校验失败、文件不可写、配置在确认期间被修改或无法可靠推导时，保留原入口并提示“隧道配置已保存，Share 入口未同步”及原因。这是两个文件的顺序保存，不是整体原子事务。“另存副本”不触发同步。

同步成功立即刷新卡片链接；运行中的 Share 以及本次改动影响的隧道仍需你手动重启生效，不会自动启停任何服务。

## 安装 frpc

在组件管理选择已有程序，或点击“获取 frpc 官方稳定版”，确认版本后下载。程序按系统架构选择官方发行文件，必须通过官方 SHA-256 验证后安装。遇到 GitHub 限流、无摘要、网络失败或校验错误，保留原程序并提供官方下载页。

每次点击都会查询 GitHub 官方 Releases 的最新稳定版，下载对应平台的已编译程序，不需要安装 Git 或拉取源码。默认安装到**客户端程序目录**的 `bin/frp/<版本>/`，确认框显示具体目录；源码启动时为项目根目录，macOS 打包后为 `.app` 外侧目录，不受终端当前目录或 `--config` 指向影响。下载校验后只提取 frpc，不额外保留压缩包。

下载成功后自动选中该程序，但不会启动或替换正在运行的进程。原有程序不会迁移、删除。若程序目录不可写或文件被占用，会提示检查权限；不会提权或自动改装到其他目录。

下载完成不会自动连接 frps。Cloudflare 首版提供本地程序选择和官方下载页。远程管理 Tunnel 在组件管理选择 token 文件模式；不支持 `--token-file` 的旧 cloudflared 必须升级或改用 YAML。

### 默认程序路径自动匹配

打开客户端后会在后台扫描以下位置，也可点击“重新扫描默认路径”：

- 客户端程序同目录（源码运行时为项目根目录）。
- 当前 `lan_config.toml` 所在目录。
- 上述目录中的 `bin` 及其最多三层子目录，例如 `bin/frp/v0.65.0/frpc.exe`。
- 没有本地候选时，查找系统 `PATH`。

支持标准 `frpc` / `cloudflared` 文件名以及当前系统、架构的平台后缀文件名，例如 `cloudflared-windows-amd64.exe`。不扫描全盘、其他项目子目录或目录链接，不把压缩包、安装器、`frps` 当作客户端程序；Linux/macOS 程序须有执行权限。每个组件的扫描最多检查 4000 个目录项，达到上限或目录不可读时提示手动选择，避免用不完整的扫描结果自动选中程序。

有效的手动路径优先；旧路径失效时保留配置并提示重新选择。只有一个完整扫描得到的候选时自动识别，多个候选时显示完整路径供你选择，不自动挑选最新版。扫描仅检查文件，不执行程序或启动连接；版本检查仍需点击“检测组件版本”。

选择或下载的程序位于配置目录内时，保存相对路径，整包移动后仍可使用；位于配置目录外时保存绝对路径。重新扫描或更换程序不会切换已运行的进程，需自行停止并重新启动对应服务后生效。

## 日志与退出

### 端口与快捷入口

服务卡片显示配置中的端口和打开按钮：Share 显示本地监听端口；frpc 区分 frps 控制端口与“本地 → 公网业务端口”映射；Cloudflare 显示 Share 回源参考端口，实际路由仍以 Tunnel 配置为准。

Share 按钮打开本地网页；frpc 和 Cloudflare 按钮分别使用 LAN 配置的 `frp_origin`、`cloudflare_origin`。未配置时禁用公网打开按钮，不把任意 TCP 映射或 frps 控制端口猜成网页。旧 `public_origin` 仍兼容：HTTPS 显示在 Cloudflare 卡片、HTTP 显示在 frpc 卡片，并在提示中注明旧配置。仅允许 HTTP/HTTPS，不自动打开浏览器。端口和链接是配置值，不代表公网已连通，配置保存后刷新显示，运行服务仍需你自行重启生效。

停止服务、超时强制停止、外部 Share 强制关闭及退出确认使用跟随当前主题的统一弹窗，明确说明目标与影响。默认选择取消，Esc、关闭叉或初始 Enter 均取消；只有明确选择确认才执行。长路径和详情可以滚动查看，不会将底部操作按钮挤出窗口。强制关闭与普通停止使用不同提示，不扩大进程管理范围。

日志可筛选、搜索、暂停显示、清空显示、导出。暂停不停止采集或服务。已知密码/token 在保存日志前脱敏；业务路径与其他内容仍需在分享前检查。

关闭窗口时，有本客户端启动的运行服务会询问是否停止并退出。超过 15 秒未结束时可以继续等待或明确强制停止。窗口退出只清理自身启动的服务，不会关闭外部 Share，不按端口或进程名杀掉其他服务。

从终端运行桌面客户端时，`Ctrl+C` 等同于请求关闭窗口：保留未保存配置及运行服务的确认，不直接打断 Qt 回调。已有模态对话框时等待其关闭后处理；连续中断不会强制杀进程或跳过清理，需强制停止时使用界面上的对应按钮。

### 外部 Share 检测与强制关闭

服务总览每 3 秒在后台检测当前 LAN 配置对应的 Share。通过 `start_lan_codex_share.cmd`、shell 脚本或另一桌面窗口启动的实例，身份和监听端口匹配后显示“运行中（外部启动）”及 PID，尚未监听时显示启动中；不会重复启动新实例。Windows Python 虚拟环境的启动器不计为另一个 Share 实例。

外部实例显示“强制关闭…”按钮。点击后需明确确认，默认取消；确认后再次核对 PID、创建时间、程序入口和配置路径。强制关闭可能中断正在执行的任务、跳过正常清理，请优先在原终端正常停止。

只关闭已验证的 Share，及同时具有该实例启动记录、实时身份和准确 App Server 命令证据的自建 Codex App Server。新版启动信息保存在配置目录的 `runtime/lan/instance.json`，不含密码/token。复用的 App Server、归属不明的旧实例子进程、PHP/Webman 和其他业务进程不处理，不递归结束整个进程树。旧 Share 无记录时仍可识别并关闭本身，但会提示关联 App Server 保留，必要时由你手动检查。

端口误占、无法读取进程信息、身份变化或多个实例无法区分时，不提供可执行的关闭操作；权限不足和超时会报告失败。外部实例退出后自动刷新，不自动重启，也不会自动接入其日志。

### 外部 frpc / Cloudflare 状态

每 3 秒后台检测外部隧道进程，Windows 额外关联服务管理器中的 PID 与服务状态。配置路径匹配后显示当前配置的外部运行状态；使用 token、默认配置或其他路径而无法确认归属时，显示“外部运行 / 配置未确认”。进程运行不代表公网连接成功，界面会注明连接未验证。

检测到外部实例、多个实例、服务启动/停止过渡状态或权限不足时，会阻止桌面重复启动并提示检查；确认不存在外部实例后才恢复启动入口。启动前再检查一次，降低缓存状态过期导致重复启动的风险。macOS/Linux 使用进程检测，不管理 systemd/launchd。

匹配当前配置的单个外部 FRP 普通进程显示“关闭外部 FRP…”按钮，确认弹窗默认取消。明确确认后再次验证 PID、创建时间、程序与配置路径，仅终止该进程，不关闭 Share、不递归结束业务子进程。超时或权限不足会报告结果，不自动升级强杀；退出后可通过“启动服务”启动当前配置。多实例、身份无法验证、Windows 服务管理的 FRP 不提供关闭权限，请在原终端或服务管理器操作；外部 Cloudflare 仍仅只读检测。

不接入外部日志。关闭桌面窗口、Ctrl+C 或保存配置均不自动关闭外部隧道。原有 `start_lan_codex_share.sh/.cmd` 只启动 Share，不联动启动 frpc 或 Cloudflare。

FRP/Cloudflare 可以单独、同时或都不启动。HTTP FRP 入口仍必须设置项目密码，浏览器到公网服务器这一段不会因 frp TLS 而变成 HTTPS。

## 打包与平台

### 外观设置

点击侧栏底部“外观设置”，可即时切换四套内置风格：

| 配置值 | 风格 |
| --- | --- |
| `graphite` | 石墨薄荷（默认深色） |
| `forest` | 暖白森林 |
| `midnight` | 午夜蓝 |
| `lavender` | 柔和紫 |

选择保存在当前 LAN 配置同目录的 `desktop_config.toml`，例如 `theme = "forest"`。下次启动自动恢复；仅更改本机桌面界面，不影响网页会话主题、配置草稿、日志或服务进程，也不需要重启服务。保存失败保留原主题并显示错误；缺省或未知主题值回退石墨薄荷。

服务总览、配置管理、组件管理、日志、操作确认和错误提示统一换肤。文件选择器和窗口标题栏保留系统原生样式，缺少 Qt 依赖时的系统错误提示也保留原有兜底方式。

### 统一应用图标

客户端使用独立的“终端 × 连接节点”标识，不使用 OpenAI 官方 Logo。窗口、任务栏及未来构建的 Windows 可执行文件/macOS 应用包使用同一套资源；切换主题不会改变应用标识，不修改配套 Codex CLI 自身的品牌图标。

矢量源为 `lan_codex_share/desktop/assets/app-icon.svg`，PNG、ICO、ICNS 都由它生成。安装桌面依赖后，可离线重新导出：

```shell
python scripts/export_desktop_icons.py
```

该命令只生成图标资源，不启动服务、不打包或发布。ICO 包含 16～256 像素多个尺寸，ICNS 包含最高 1024 像素资源。PyInstaller 的桌面 spec 包含图标运行资源，源码运行及冻结程序都不依赖当前工作目录来查找图标。

### 发行平台

参见 [流水线文档](PIPELINE_RELEASE.md)。一个桌面发行包包含 GUI、配套 Share/CLI、示例配置和第三方许可，原有命令行入口不变。

GUI 使用 Qt 6.11.2：目标 Windows 10+、macOS 13+；Linux x64 的 wheel 要求 glibc 2.34+，ARM64 要求 glibc 2.39+，另需桌面显示环境及平台库。其他平台兼容性以实际构建与测试结果为准。未签名/公证时系统可能提示未知发布者。
