# frpc 最新稳定版默认安装目录

日期：2026-09-16。状态：已确认并实现，全量回归 391 项通过；未提交、未打包发布。

## 行为

“获取 frpc 官方稳定版”沿用 GitHub 官方 Releases latest 查询，按当前操作系统和 CPU 架构选择已编译发行包，不拉取源码、不安装 Git、不现场编译。

唯一目录变更：将默认安装根目录由 LAN 配置目录下的 `bin/frp` 改为客户端程序目录下的 `bin/frp`；最终程序位于 `bin/frp/<版本>/frpc.exe`（Windows）或 `bin/frp/<版本>/frpc`（macOS/Linux）。不额外保留下载压缩包。

程序目录复用 `distribution_directory()`：源码启动为项目根目录；Windows/Linux 打包后为程序所在目录；macOS 为 `.app` 外侧发行目录。不会随终端工作目录或 `--config` 指向变化。

确认下载的对话框显示版本和实际目标目录。保留后台下载、进度、取消、官方 SHA-256 校验、安全解压及已有同版本文件保护；目录不可写时明确提示，不静默换目录、不请求提权。成功后沿用程序选择保存逻辑，自动选中 frpc，但不启动连接、不切换已运行进程。

既有程序和手动配置不迁移、不删除。配置目录以内的程序保存相对路径，外部程序保存绝对路径，保持此前路径规则。

## 验证和边界

通过模拟发行数据和安装器验证：配置目录与程序目录不同、终端工作目录不同的情况下，仍传入程序目录下的 `bin/frp`；对话框显示目标；下载完成后的保存路径正确；取消或不可写不改变原选择。运行桌面测试与全量回归。

仅修改 lan_codex_share 项目。此次不启动服务、不实际连接 frps、不提交、不打包发布。
